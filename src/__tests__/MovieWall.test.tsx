

import MovieWallProps  from "../components/elements/MovieWall";

describe("<movieWall />", () => {
  test("should display a wall of movies", async () => {
    expect(MovieWallProps).not.toBe(null)
  });
});